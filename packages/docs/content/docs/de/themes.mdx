---
title: Themes
description: Passe ZedCode mit eingebauten und benutzerdefinierten Themes an.
---

# Themes

ZedCode unterstützt eingebaute Themes und benutzerdefinierte Theme-JSON-Dateien.

## Ein benutzerdefiniertes Theme hinzufügen

1. Erstelle das Themes-Verzeichnis:

```bash
mkdir -p ~/.config/zedcode/themes
```

2. Füge deine JSON-Datei in dieses Verzeichnis ein (zum Beispiel `my-theme.json`).
3. Öffne ZedCode und gehe dann zu **Einstellungen -> Theme -> Themes neu laden**.
4. Wähle dein Theme im Dropdown aus. Es wird sofort angewendet.

## Speicherort der Themes

- macOS/Linux: `~/.config/zedcode/themes/`

## Vollständige Referenz des JSON-Formats

Verwende die vollständige Formatbeschreibung in den Haupt-Repository-Dokumenten:

- [`docs/CUSTOM_THEMES.md`](https://github.com/zedcode/zedcode/blob/main/docs/CUSTOM_THEMES.md)

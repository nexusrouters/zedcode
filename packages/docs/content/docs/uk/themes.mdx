---
title: Теми
description: Налаштуйте ZedCode за допомогою вбудованих і власних тем.
---

# Теми

ZedCode підтримує вбудовані теми та власні JSON-файли тем.

## Додати власну тему

1. Створіть директорію тем:

```bash
mkdir -p ~/.config/zedcode/themes
```

2. Додайте свій JSON-файл у цю директорію (наприклад, `my-theme.json`).
3. Відкрийте ZedCode, потім перейдіть до **Settings -> Theme -> Reload themes**.
4. Оберіть свою тему зі спадного списку. Вона застосується одразу.

## Розташування тем

- macOS/Linux: `~/.config/zedcode/themes/`

## Повний довідник формату JSON

Скористайтеся повним посібником із формату в доках основного репозиторію:

- [`docs/CUSTOM_THEMES.md`](https://github.com/zedcode/zedcode/blob/main/docs/CUSTOM_THEMES.md)
